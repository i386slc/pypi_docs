# click

**Click** — это пакет Python для создания красивых интерфейсов командной строки компонуемым способом с минимальным количеством кода. Это _«Комплект для создания интерфейса командной строки»_. Он легко настраивается, но поставляется с разумными настройками по умолчанию.

Он направлен на то, чтобы сделать процесс написания инструментов командной строки быстрым и увлекательным, а также предотвратить любое разочарование, вызванное невозможностью реализовать предполагаемый API CLI.

Click в трех точках:

* произвольная вложенность команд
* автоматическая генерация справочной страницы
* поддерживает ленивую загрузку подкоманд во время выполнения

На что это похоже? Вот пример простой программы **Click**:

```python
import click

@click.command()
@click.option('--count', default=1, help='Number of greetings.')
@click.option('--name', prompt='Your name',
              help='The person to greet.')
def hello(count, name):
    """Simple program that greets NAME for a total of COUNT times."""
    for x in range(count):
        click.echo(f"Hello {name}!")

if __name__ == '__main__':
    hello()
```

И как это выглядит при запуске:

```bash
$ python hello.py --count=3
Your name: John
Hello John!
Hello John!
Hello John!
```

Он автоматически генерирует красиво отформатированные страницы справки:

```python
$ python hello.py --help
Usage: hello.py [OPTIONS]

  Simple program that greets NAME for a total of COUNT times.

Options:
  --count INTEGER  Number of greetings.
  --name TEXT      The person to greet.
  --help           Show this message and exit.
```

Вы можете получить библиотеку прямо из PyPI:

```bash
pip install click
```

## Документация

Эта часть документации проведет вас через все шаблоны использования библиотеки.

* Почему Click?
* Быстрый старт
* Интеграция setuptools
* Параметры
* Опции
* Аргументы
* Команды и группы
* Подсказки ввода пользователя
* Документирование скриптов
* Сложные приложения
* Расширенные шаблоны
* Тестирование приложений Click
* Утилиты
* Доработка оболочки
* Обработка исключений
* Поддержка юникода
* Примечания к консоли Windows

## Справка API

* Декораторы
* Утилиты
* Команды
* Параметры
* Контекст
* Типы
* Исключения
* Форматирование
* Парсинг
* Доработка оболочки
* Тестирование

## Разные страницы

* click-contrib
* Обновление до новых выпусков
* Лицензия BSD-3-Clause
* Изменения
